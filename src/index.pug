doctype html
html(lang="en")
    head
        meta(charset="utf-8")
        title 
        meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
    include components/body/body.pug
        include components/header_login/header_login.pug
      
        include components/color&type/color&type.pug
        +title_h1('This one is the sub-section or widget title')
        +title_h2('Next one is the item title inside widgets')
        +title_h3('This is a label or CTA text')

        //- include components/header_en/header_en.pug
        br
        br
        include components/form_elements/form_elements.pug
        
        
    
        